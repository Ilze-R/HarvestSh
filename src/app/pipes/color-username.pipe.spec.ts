import { ColorUsernamePipe } from './color-username.pipe';

describe('ColorUsernamePipe', () => {
  it('create an instance', () => {
    const pipe = new ColorUsernamePipe();
    expect(pipe).toBeTruthy();
  });
});
